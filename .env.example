JWT_SECRET_KEY=change-me-in-production

# MongoDB Atlas connection
MONGO_URI=mongodb+srv://santharam:Abc123@cluster0.f86u3fe.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=map_backend

# Gmail SMTP — santharam2570@gmail.com
# Google does NOT allow your normal Gmail password for SMTP.
# You must use a Google App Password (16 characters):
#   1. https://myaccount.google.com/security → turn on 2-Step Verification
#   2. https://myaccount.google.com/apppasswords → create password for "Mail"
#   3. Paste the 16-char code below (no @ symbol, looks like: abcd efgh ijkl mnop)
MAIL_ENABLED=true
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=santharam2570@gmail.com
MAIL_PASSWORD=fxrhkxnmeibcuoik
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_DEFAULT_SENDER=santharam2570@gmail.com

# Show OTP in terminal if email fails (local testing)
DEV_LOG_OTP=true

# AI chat (POST /ai_chat) — free Hugging Face Inference API
# 1. Sign up: https://huggingface.co/join
# 2. Create a Read token: https://huggingface.co/settings/tokens
AI_PROVIDER=huggingface
HF_TOKEN=
HF_MODEL=HuggingFaceH4/zephyr-7b-beta

# Optional paid provider — set AI_PROVIDER=openai
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini
